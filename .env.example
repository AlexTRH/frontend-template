# Base URL for API (optional in dev; in production without MSW set this or requests will fail)
# VITE_API_URL=http://localhost:3000

# Enable MSW in dev: list and create items work without backend
# VITE_USE_MSW=true
