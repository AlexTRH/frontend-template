/**
 * Пример общих констант приложения.
 * Слайс-специфичные константы (например ITEMS_QUERY_KEY) хранятся в своих слоях.
 */

export const DEFAULT_PAGE_SIZE = 10

export const STORAGE_KEYS = {
    AUTH: 'auth',
    THEME: 'theme',
} as const
